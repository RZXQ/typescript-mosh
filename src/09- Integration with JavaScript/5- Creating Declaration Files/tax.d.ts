// Declaration file (.d.ts) provides type definitions for JavaScript files
// Naming convention: same filename with .d.ts extension (e.g., tax.js → tax.d.ts)
// tax.js (implementation) + tax.d.ts (type definitions) = tax.ts (TypeScript with both)

export declare function calculateTax(income: number): number;
